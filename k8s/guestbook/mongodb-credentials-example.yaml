# Template for MongoDB credentials
# Copy to mongodb-secret.yaml and fill in your values
# DO NOT commit the actual secret file

apiVersion: v1
kind: Secret
metadata:
  name: guestbook-mongodb-credentials-example
  namespace: default
type: Opaque
stringData:
  connection-string: "mongodb://USERNAME:PASSWORD@mongodb-service:27017/?authSource=admin"